#include "main.h"

/**
 * print_sign - returns 1 if n > 0
 *  return 0 if n = 0
 *  return -1 if n < 0
 * @n: number to check
 * Return: 1 if n > 0, 0 if n = 0 and -1 if n < 0
 */

int print_sign(int n)
{
	if (n > 0)
		return (1);
	else if (n == 0)
		return (0)
	else
		return (-1);
}
